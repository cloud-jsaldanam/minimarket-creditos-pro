{
  "name": "minimarket-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../api && npm install",
    "start": "echo 'Desplegado en Azure'"
  }
}